# 4.2.0 文档核对表：旧文案位置 → 实际代码/公开类型证据 → 修订结果

> 2026-09-25。基线 = 4.1.0 发布包（`@fulgurjs/federation@4.1.0`）与 master `9ba55ed` 时的文档。
> 证据列均为**执行时实际核对的源码/行为**，不是推断。覆盖范围：仓库根 README（npm 包内 README
> 由构建脚本 `cp ../../README.md` 复制，同源）、docs/迁移指南.md、CLI `--help`、`init` 输出、
> examples/、MES 项目侧文档（src/fulgurjs/README.md × 3、旧实施方案 §11）。

| # | 旧文案位置（4.1.0） | 实际代码 / 公开类型证据 | 修订结果（4.2.0） |
|---|---|---|---|
| 1 | README §「单配置驱动 Vite：federationOptionsForApp」把 `loadRepoConfig + federationOptionsForApp + federation` 作为推荐接法 | `packages/plugin/src/config.ts` 的两层转换器仍在（兼容保留）；MES 三应用 4.1.0 实际接入即该写法 | 章节重写为「每项目一份配置：fulgurjs.config.ts + federation(fulgurjsConfig)」单项目主路径；三层转换降级为明确标注的「历史兼容（不推荐）」段 |
| 2 | README §4「fulgurjs.config.ts」以 `root + apps[]` 聚合配置为默认形态 | `config.ts:RepoConfig` 要求 `root` + 非空 `apps[]`；单项目仓库被迫假装多项目根 | §4 重写：默认形态 = 默认导出直接是 `FederationOptions`（`satisfies` 检查）+ 可选 `hostPages` 具名导出；聚合配置移入「历史兼容」段 |
| 3 | README §CLI 推荐「init 打印后手工粘贴」与转换器 | `cli.ts` 实际行为：`init --config` 打印接入块 + 核对清单；单项目形态打印 `federation(fulgurjsConfig)` 两行接法 | CLI 章节按实际输出重写（本地实测：init/help/inspect 三命令逐条跑过） |
| 4 | README §9.1 声称 `host.prefetch`（`'all' \| string[] \| false`）可写进 fulgurjs.config.ts 且「init 生成 bridge.ts 时注入」 | `options.ts:FederationOptions` 无 `prefetch` 字段（全部字段逐一核对）；`HostConfig`（config.ts）亦无；`init.ts` 只写 fulgurjs.config.ts 模板，从不生成 bridge.ts | §9.1.3 表格删除 `host.prefetch` 行，加订正说明「插件配置面没有该字段」；预载名单唯一入口 = 宿主桥 `PREFETCH_REMOTES` 常量（bridge.ts 实码存在该常量） |
| 5 | README §9.1 引言「这些能力随 fulgurjs init 生成的模板直接带出」；迁移指南 §三E 同款（含「删已生成产物重跑 init」操作指引） | `init.ts` 全文核对：`writeConfigTemplate` 只写配置模板文件；无任何项目源码生成逻辑 | 引言改为「项目侧能力（手工落位），init 只生成配置起步模板」；「删产物重跑 init」指引删除 |
| 6 | README §7「NGINX 部署模板用 fulgurjs init --config 自动生成」 | `init.ts:nginxSnippetFor` 仅 return 字符串供打印；无文件写入 | 改为「打印样板（不写文件），仅随旧聚合配置输出」 |
| 7 | README §「配置出错」节写「五段共 35 个」错误码，与 §6 表「41 个」自相矛盾 | `scripts/check-manual-codes.mjs` 构建门禁实测输出「登记 41 = 文档 41」 | 统一为 41，锚点同步修正 |
| 8 | 迁移指南 §二步骤 3 引用 `admin/src/qiankun/fulgurjsPages.ts` | MES 实码路径为 `cku-mes-admin/src/fulgurjs/host/pages.ts`；qiankun 目录是旧乾坤注册层 | 指向实际路径，并补「页面数据唯一源 pages.data.ts + hostPages 具名导出」说明 |
| 9 | 迁移指南 §目录约定「远程应用为 exposes/（联邦启动引导 federatedBoot + …）」 | 4.1.0 起 federatedBoot 已删除（MES 实码无此文件）；初始化 = `setup.ts` 经 `federation({ setup })` | 改为 `exposes/ + setup.ts`；并注明这些是项目手工维护文件，init 不生成不重建 |
| 10 | 迁移指南头部案例写「testbed/demo-app」 | 当前真实工程 = `testbed/mes-zc`（admin/bpm/lowcode） | 更正案例名 |
| 11 | 旧方案 `docs/宿主接入简化与MES-ZC桥接修正实施方案.md` §11 称旧 `federatedBoot` 链路为「当前代码」 | 4.1.0 起当前链路 = setup/onSession 生命周期（runtime/index.ts:ensureLifecycle/runSetupPhase）；federatedBoot 为 4.0.0 历史 | §11 标题与开头加显著「4.0.0 历史链路（仅存档）」横幅，指明当前链路出处 |
| 12 | MES 三应用 7 个源码文件头带「[fulgurjs:init] generated — 可用 fulgurjs init 幂等重建」 | `init.ts` 不生成任何项目源码；这些文件（fulgurjs README × 3、FulgurjsDemo.vue、TaskCard.vue、InfoCard.vue、moduleDesign/index.vue）是手工业务文件 | 标记改为「项目手工维护的联邦业务文件（fulgurjs init 不生成、不重建项目源码）」 |
| 13 | MES 三应用 `src/fulgurjs/README.md` 自称「迁移工具脚手架」 | 同上，均为手工维护 | 归属改「项目手工维护」；admin 版补页面数据唯一源（pages.data.ts）与项目根 fulgurjs.config.ts 位置说明 |
| 14 | `examples/fulgurjs.config.example.ts`（随 npm 包发布）为聚合配置形态 | 包构建把根 examples/ 原样复制进包 | 重写为单项目形态（remote/host 两份默认导出 + satisfies + hostPages 注释示例） |
| 15 | README 徽章 tests-355、§开发与测试「单测（188）」 | vitest 实测 369 passed（本轮含 app-config.test.ts 新增） | 更新为 369 |
| 16 | CLI `--help` 无单项目形态说明、check-pages 无 `--manifest`/`--require-verified` | `cli.ts` 重写后的实际输出（实测 head 12 行见交付报告） | 重写：单项目默认用法 + 免 --app + 新参数 + 聚合兼容注记 |
| 17 | `init` 输出「按清单把 federation(federationOptionsForApp(...)) 接入各应用 vite.config.ts」 | 实测 init 输出（见交付报告）为两行接法 + hostPages 提示 | 已重写（见 #3） |
| 18 | CHANGELOG | 保留版本演进史原则 | 新增 4.2.0 条目（新能力 + 行为变更），4.1.0 及更早原样保留 |

## 安装版本 / 导入路径 / API 面核对（与发布包一致）

| 项 | 证据 | 状态 |
|---|---|---|
| 包根导出 | `packages/plugin/package.json` exports：`.`（types/import/require）、`./config`、`./runtime`、`./package.json`、`./internal/*` | README「唯一 API 入口」描述与之一致 |
| `FederationOptions` 类型 | `src/index.ts:1031` `export type { FederationOptions } from './options'` → dist/index.d.ts | 单项目 `satisfies` 依赖成立；发布后另经 registry fixture 的 tsc 类型检查复验 |
| README 文档章节链接 | `docs/迁移指南.md`、`webpack-mf-对照与缺口.md`、`沙箱边界审计.md`、`P5-vite7-8兼容矩阵.md` 均在包 files 列表且 build 脚本复制 | 一致 |
| npm 包内 README | build 首步 `cp ../../README.md README.md` → 与仓库根同源 | 发布后另做包内容抽查（见 fixture 验证） |

## 示例可运行性验证

| 示例 | 验证方式 | 结果 |
|---|---|---|
| README 路径①（普通 TS expose + Vue 组件 + loadRemote/remoteComponent） | e2e fixtures/host-vue + remote-a（link: 插件 dist）dev e2e 用例 | e2e 通过（本轮 CI/本地见发布门禁） |
| README 路径③（setup + onSession） | fixtures/remote-auto（setup fixture，4.1.0 起在库） | e2e 通过 |
| 单项目 fulgurjs.config.ts 主路径 | 插件单测 app-config.test.ts（26 用例：加载/形状/路径逃逸/角色判定/CLI）+ 真实 MES 三配置 `explain`/`check-pages` 实跑 | 全绿；MES explain 双角色 + 26 页映射 + manifest 来源显示正确 |
| init 模板 | `/tmp` 干净目录实测 `init` → 模板可被 `loadAppConfig` 读取；expose 目标不存在时三段式报错（诚实失败） | 通过 |
