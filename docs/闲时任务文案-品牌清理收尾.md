# 闲时任务文案：品牌清理收尾续作（2026-09-16 晚）

> 用法：把下面分隔线内的全文粘贴给闲时任务即可。文案自包含，执行者无需本会话上下文。

---

## 任务：fulgur-federation 插件品牌清理收尾续作（接上一轮，按交接文档 A→G 执行）

### 背景与必读文件（按序完整阅读后再动手）

仓库：`/Users/Admin/Desktop/ai 杂物/插件/fulgur-federation/`（git，本地领先 origin 8 笔提交，**全部未推**）

1. `docs/品牌清理与全量回归任务书.md` —— 总纲（含 §9 总验收清单）
2. `docs/品牌清理续作交接-2026-09-16.md` —— **上一轮执行快照，你的工作清单在这里**：阶段一~五已完成并本地提交，你从「三、未完成清单」A→G 顺序执行
3. 参考：`docs/27页双环境矩阵.md`（验收对照基线）、`docs/遗留任务交接清单.md`（历史坑全集）

### 上一轮已完成（不要重做）

- 阶段一~四全绿：目录改名 fulgur-federation、unifed 全清零、单测 97/97 + fixtures dev e2e 10/10 + prod e2e 8/8 + H7 装后实测、NGINX 8662 站点重建冒烟全过
- 阶段五 testbed 已从 demo-app 重拷并完成联邦改造（admin 四件套 + bpm/lowcode exposes + federatedBoot），**dev 27/27 矩阵 + 全链路闭环 5/5 + CRUD 五页 + 动作 8/8 全绿；prod 构建部署 8662 后 26/27 + 三套闭环全绿**
- admin prod 构建三件套规避已落地（**不可回退**）：`sourcemap: false` + 停用 `vite-plugin-top-level-await` + `build.target: 'es2022'`
- 8662 conf 深链回退宿主已修、matrix-shot 登录等待已加固

### 执行清单（按序，每项验证绿了才算完）

**A. prod lowcode 设计器页 TypeError（最先做）**
现象：prod 矩阵 22~27 页 `pageErrors: ["TypeError: p.default.extend is not a function"]`（dev 同 6 页无报错）。
先做对照实验再动手：本轮 lowcode `vite.config.ts` 新增了 `dayjs → dayjs/esm` 别名；而历史记录（commit fa48c4c / d3af5de 教训）表明**该别名在 prod rollup 下会把 element-plus 的 CJS require(dayjs) 导向 ESM 双重 interop 丢静态方法**，上一轮的解法是撤别名。步骤：
1. 撤 lowcode 的 dayjs 两条例名 → `rm -rf node_modules/.vite` → 重跑 `cd demo-lowcode && pnpm run build` → 重新 rsync 部署 → 重跑 `VBASE=http://localhost:8662 VTAG=prod node e2e/matrix-shot.mjs` 看 22~27 pageErrors
2. 若错误消失 → 定案为别名引入，撤别名收尾；若依旧 → 是历史 U-2 prod 既有签名（可接受，u2-blank 页保持路由可达即可），如实记录勿强修
3. 注意 dev 侧：若撤别名导致 dev 的 lowcode 出现 dayjs localeData 类报错，改用更精细方案（勿整体回退）

**B. 截图证据收尾（任务书阶段六）**
1. `/tmp/tb-demo.png`、`/tmp/tb-demo-clicked.png`、`/tmp/tb-smoke.png`、`/tmp/tb-detail.png` 拷入 `docs/screenshots/` 改名 `tb-dev-fulgur-demo.png`、`tb-dev-fulgur-demo-advanced.png`、`tb-dev-todo-federated.png`、`tb-dev-detail-timeline.png`
2. `docs/manual.html` 引用截图逐张核对：`tb-dev-demo-page / tb-dev-remotes-loaded / tb-dev-scope-negotiation / tb-prod-minimal-host / tb-dev-hmr-before / tb-dev-hmr-crossapp / t5-dev-02-amis-detail / t5-prod-02-amis-detail` 等——缺的重拍（需写小截图脚本：scope 协商网络面板、prod 最小宿主 runtime API 页、bpm 改源码→admin 页 HMR 热更）；所有含旧品牌字样的截图必须被新图覆盖
3. 重跑归档：`node e2e/pending-routes-shot.mjs`（dev+prod 各一次）、`VTAG=prod VBASE=http://localhost:8662 node e2e/amis-dev-verify.mjs`（遗留2复验 iframe=0）、`node e2e/collect-errors.mjs`（刷新 console-errors JSON）、`VBASE=http://localhost:8662 node e2e/tb-demo-shot.mjs`（演示页 prod）

**C. 两个待确认项**
1. prod 矩阵 06-detail iframe=1：实例是 AMIS 类型。确认该 iframe 是 amis 渲染器内部产物还是业务表单区回落了 iframe 通道（遗留 2 断言 iframe=0）；若是回落，查 prod 下 `window.mainAppProps` 写入时序 vs detail 模块求值时序
2. dev 矩阵 06 textLen=226（历史 349）：探针实测时间线 3 节点完整渲染，判定为冷转换等待差异即可，不必修

**D. 插件改进项（评估并在 docs 记录排期建议，非必做）**
1. 远程 SFC 静态导入 `virtual:fulgur-runtime` 的 dev 检测与显式报错（实测静默破坏渲染上下文，排障 40 分钟）
2. 带参路由 expose 键冲突告警（`:type/:id` 剥参后与列表页键收敛）
3. `globalThis.__FULGUR_RUNTIME__` 官方化（getRuntime() 导出或文档化契约）

**E. git 收尾**：上述修复逐项分阶段本地提交（延续现有提交信息风格）

**F. 阶段七**：grep 核对 `fulgur-remoteEntry.js / fulgur-manifest.json / 8662 / 8773 / 4529 / 4669 / fulgur-demo` 在 README、packages/plugin/README.md、DESIGN.md、docs/manual.html、docs/迁移指南.md、docs/遗留任务交接清单.md 与实现一致；DESIGN.md 已知差异 #7 补记三件套规避实测解；输出总结报告（任务书 §9 清单逐项勾选：单测数/e2e 数/27 页双环境结果/截图清单/后台失联影响项/遗留问题）

**G. 总验收**：任务书 §9 逐项勾选，未勾项说明原因

### 红线（全程有效，优先级最高）

1. demo-app 原项目（`/Users/Admin/Desktop/svn_project/demo-app/`）**只读**，绝不修改，绝不执行任何 svn 命令
2. git **只本地提交，绝不 push**；全部完成后停下等用户确认
3. shell 有代理 127.0.0.1:7890：凡 localhost 的 curl 加 `--noproxy '*'`，浏览器自动化先设 `NO_PROXY=localhost,127.0.0.1`
4. 后台 localhost 已失联（**本地 localhost:8085 活着，用本地**）：业务接口报错若确属后台问题，截图标注"环境问题（后台失联），非插件问题"，禁止伪造数据
5. 改插件 dist 或任何 vite 配置后：`rm -rf` 三应用 `node_modules/.vite` + 重启 dev server
6. 所有功能验证必须浏览器真实打开页面 + 截图留证；**验证不绿必须停下排查，禁止跳过验证继续**

### 环境速查

- 后台：`http://localhost:8085/demo`（admin / Demo@123456，UI 登录免验证码）
- dev 三服务：admin 8773（/main）/ bpm 4529（/flowable）/ lowcode 4669（/lowcode）；若已停：`cd testbed/demo-app && pnpm start`（后台拉起，冷启动首轮 30s+ 转换属正常，先预热再断言）
- prod 站点：NGINX 8662（root `/opt/homebrew/var/www/fulgur-test`，conf `e2e/nginx/30-fulgur-test-8662.conf`）；部署命令在交接文档「四、坑指引」
- 插件 dist 已构建（packages/plugin/dist）；三应用经 `link:../../../packages/plugin` 软链消费

### 关键坑（详见交接文档「四、坑指引」7 条，此处最高频 3 条）

1. 远程页面 SFC **禁止**静态 `import 'virtual:fulgur-runtime'`（破坏渲染上下文），走 `globalThis.__FULGUR_RUNTIME__`
2. bpm/lowcode 冷启动首轮访问会出现瞬时假错误（预构建未就绪），**第二轮起干净，勿当回归**
3. 浏览器 persistent profile 有缓存：验证配置变更必须换全新 profile

### 完成标准

任务书 §9 总验收清单全部可勾选（唯一允许例外：lowcode 6 页 U-2 渲染空白为 avue UMD 已知限制，但 pageErrors 必须归零）；全部本地提交；输出总结报告后**停止，等待用户指示，不 push**。
