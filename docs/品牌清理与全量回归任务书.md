# 品牌清理收尾 + 全量回归任务书（v0.2 唯一未竟事项）

> 创建：2026-09-16。前置：提交 `aee36d6`（unifed→fulgur 源码全量替换，单测 97/97 + fixtures dev 10/10 + prod 8/8 已绿）与 `8303365`（对外文档补漏）已完成并推送。
> 本任务书覆盖剩余全部 `unifed` 残留清理 + 改名后的**全量双环境重新测试、重新打包、重新部署、截图证据重产出**。
> 执行者注意：**所有步骤做完才算完成**，任何一步验证不绿必须停下排查，禁止跳过验证继续往下走。

---

## 0. 现场事实（2026-09-16 核实）

| 项 | 状态 |
|---|---|
| 本地仓库目录 | `/Users/Admin/Desktop/ai 杂物/插件/vite-plugin-unifed/`（git，remote = github.com/chenmingye/fulgur-federation） |
| NGINX 8662 测试站点 conf | **已被用户删除**（servers/ 下仅剩用户自有 8661/8773 站点），需按 §5 重建 |
| `/opt/homebrew/var/www/unifed-test`、`unifed-fresh` | 旧站点目录**还在**，需删除并以新名重建 |
| 工作区旁 `fresh-testbed/` 副本 | 已被用户删除 |
| 仓库内 `testbed/demo-app/` 副本 | 已被删除；**testbed 从未入 git（.gitignore 排除 7.6G 副本），无法恢复，必须按 §6 重新拷贝接入** |
| demo-app 原项目 | `/Users/Admin/Desktop/svn_project/demo-app/demo-monorepo/`（admin/bpm/lowcode 齐全），**只读，绝不修改、绝不 svn 操作** |
| 后台 localhost:8085 | ⚠️ 2026-09-14 起失联。prod 业务接口验证受阻时按 §5 降级口径处理，属环境问题非插件问题 |
| shell 代理 | ⚠️ 用户 shell `http_proxy=127.0.0.1:7890`，跑 localhost 相关命令前必须 `NO_PROXY=localhost,127.0.0.1`，否则出现 502 假象 |

## 1. 红线（全程有效）

1. demo-app 原项目**只读**；所有副本与 SVN 无关，**绝不执行任何 svn 命令**。
2. git **只本地提交，不 push**。分阶段提交（见 §8），全部完成后停下向用户汇报，推送等用户指示。
3. 改完插件 dist / 改动 vite 配置后，所有 dev server 必须重启 + `rm -rf node_modules/.vite`（immutable 缓存红线）。
4. 拷贝副本 rsync 排除 `node_modules`；副本内独立安装（pnpm 若询问构建脚本按原项目 allowBuilds 口径处理）。
5. 浏览器自动化跑 localhost 时带 `NO_PROXY=localhost,127.0.0.1`。
6. 每个功能验证必须浏览器真实打开页面 + 截图留证（DESIGN.md §6.7 规范），禁止只看 console 下结论。

## 2. 阶段一：目录改名 + 全局路径同步

**目标名：`fulgur-federation`**（与 GitHub 仓库名一致，符合"不要 vite 开头"的品牌约定）。

1. `mv "/Users/Admin/Desktop/ai 杂物/插件/vite-plugin-unifed" "/Users/Admin/Desktop/ai 杂物/插件/fulgur-federation"`
2. 全仓库（排除 node_modules/.git/dist）把绝对路径 `/Users/Admin/Desktop/ai 杂物/插件/vite-plugin-unifed` 统一替换为新路径。已核实波及 15 文件：`e2e/nginx/nginx.conf`、`e2e/u2-prod-verify.mjs`、`e2e/flow-closure.mjs`、`e2e/pending-routes-shot.mjs`、`e2e/func-suite.mjs`、`e2e/d1-closure.mjs`、`e2e/d2-shot.mjs`、`e2e/h5-diff.mjs`、`e2e/t6-shot3.mjs`、`e2e/matrix-shot.mjs`、`e2e/prod-verify-t2.mjs`、`e2e/amis-dev-verify.mjs`、`e2e/model-actions.mjs`、`e2e/collect-errors.mjs`、`docs/遗留任务交接清单.md`（以执行时 grep 实际结果为准）。
3. 验证：全仓库 grep 旧目录名 `vite-plugin-unifed` 应为 0 命中（node_modules/dist 除外）；`git -C 新目录 status` 正常。

## 3. 阶段二：代码与配置清理（monorepo 内）

对下列文件执行 `unifed→fulgur`、`UNIFED→FULGUR` 通用替换（替换后必须逐文件 grep 复核语义，特别注意 URL 与产物文件名拼接处）：

| 文件 | 已核实残留 | 说明 |
|---|---|---|
| `package.json`（根） | name `vite-plugin-unifed-monorepo` | 改为 `fulgur-federation-monorepo` |
| `e2e/package.json` | name `unifed-e2e` | 改为 `fulgur-e2e` |
| `e2e/playwright.config.ts` | 2 处 webServer 健康检查 URL `/@unifed-manifest.json` | **不改 e2e 起不来**（端口 ready 探测 404）；改 `/@fulgur-manifest.json` |
| `e2e/nginx/nginx.conf` | 9 处：`/tmp/unifed-nginx/*`（pid/log）、产物名 `unifed-remoteEntry.js` ×2、绝对路径 ×5 | tmp 目录改 `/tmp/fulgur-nginx`；产物名改 `fulgur-remoteEntry.js`（与 `options.ts DEFAULT_FILENAME` 一致，**当前此 conf 指向旧产物名，prod e2e 实际已坏**） |
| `e2e/` 其余 18 个脚本 | 各 1~2 行（sandbox-audit 5 行）：注释/断言 URL/日志前缀 | 全目录统一替换 |
| `fixtures/host-vue/src/unifed-types/` | **孤儿旧目录**（aee36d6 已新建 `fulgur-types/` 并存，新代码全部指向 fulgur-types） | 直接整目录删除；删除后 grep `unifed-types` 确认 0 引用残留；顺带检查 `fulgur-types/*.d.ts` 文件头注释里的旧品牌字样 |
| `docs/` 6 个历史文档 | 沙箱边界审计 1、H3零兜底审计报告 3、webpack-mf-对照 1、遗留任务交接清单 17、插件成型路线图 4、待办问题与迁移方案 17 | 通用替换；其中 NGINX conf 路径 `30-unifed-test-8662.conf` 的历史描述随 §5 新名更新为 `30-fulgur-test-8662.conf` |
| DESIGN.md:130 | `30-unifed-test-8662.conf`（仓库托管的 conf 描述） | 随 §5 新名更新 |

替换完成后全仓库大小写不敏感 `grep -ri unifed`（排除 node_modules/.git/dist/*.png），应仅剩本任务书与 git 历史。

## 4. 阶段三：单测 + fixtures 双环境 e2e 回归

```bash
pnpm install && pnpm --dir packages/plugin build   # 重新构建 dist（改名后必须重打）
pnpm test   # = 单测 97 + fixtures dev e2e 10 + prod e2e 8
bash e2e/h7-install-test.sh   # H7 装后实测：npm pack → 干净目录安装 → dev+prod 双引擎断言
```

- 全部必须绿；prod e2e 走重建后的 `e2e/nginx/nginx.conf`（8999 隔离实例）。
- 此阶段产物（dist/tarball）就是后续 testbed/NGINX 部署用的包，禁止用旧 dist。

## 5. 阶段四：NGINX 生产站点重建（新品牌）

1. 删除旧站点目录 `/opt/homebrew/var/www/unifed-test`、`unifed-fresh`（旧品牌残留，用户已明确废弃）。
2. 新建 `/opt/homebrew/etc/nginx/servers/30-fulgur-test-8662.conf`：独立端口 **8662**，不碰用户自有 8661/8773 站点；root `/opt/homebrew/var/www/fulgur-test`；`/main`、`/flowable`、`/lowcode` try_files SPA 回退；`/demo` 反代 `http://localhost:8085`（含 WebSocket Upgrade 三件套）。conf 托管副本存入仓库 `e2e/nginx/30-fulgur-test-8662.conf`。
3. `nginx -t` 通过 → reload → curl 冒烟（页面 200、remoteEntry 200、manifest 200、CORS 头、gzip）。
4. **后台失联降级口径**：若 localhost:8085 仍不可达，静态链路（remoteEntry/manifest/CORS/gzip/页面框架/联邦加载）照常验证；依赖后台的业务接口报错在截图中标注"环境问题（后台失联），非插件问题"，不得伪造接口数据。

## 6. 阶段五：testbed 真实项目副本重建（从 demo-app 重新拷贝接入）

> testbed 旧副本已被删且不入 git。以下端口号沿用历史基线（DESIGN.md §6.2）：admin dev **8773**（若与用户新增的 NGINX `20-touch-8773.conf` 冲突则顺延换端口并同步 e2e 脚本）、bpm **4529**、lowcode **4669**。

1. `rsync -a --exclude node_modules /Users/Admin/Desktop/svn_project/demo-app/demo-monorepo/ testbed/demo-app/`，副本内三应用独立 `pnpm install`。
2. 按 `docs/迁移指南.md` 七步法 + `docs/待办问题与迁移方案.md` §2.2 改造清单做联邦接入，**命名一律 fulgur**：
   - admin：`src/qiankun/fulgurPages.ts`（27 页路由映射表 + `resolveFulgurPageFromPath`）、`fulgurBridge.ts`（token/user 桥）、`types/fulgur-runtime.d.ts`、`src/fulgur-types/`（dev 类型直连产物）
   - bpm/lowcode：`src/fulgur-exposes/<页>.vue` exposes 化、`federatedBoot.ts`（全局注册，失败显式报错）、vite.config 接入 `federation()`（vue/pinia singleton；element-plus 等业务库**不进** shared，走插件 UMD/CJS 自动处理）
   - admin 免登录演示路由 `/main/fulgur-demo`（meta.ignoreAuth）+ 页面联邦通道（原 qiankun 集成保留不动，平行通道）
3. 接入验证（dev，三 server 并跑，浏览器逐页）：27 页 dev 路由级渲染 + 写操作闭环抽测 + `unifed-demo`→`fulgur-demo` 演示页 + console 零报错。参考 `e2e/func-suite.mjs`、`flow-closure.mjs`、`model-actions.mjs` 的路由/动作断言。
4. prod：三应用 `pnpm build` → dist/{main,flowable,lowcode} 部署到 §5 的 8662 站点 → 27 页 prod 逐页回归（`docs/27页双环境矩阵.md` 为验收对照）。

## 7. 阶段六：截图证据全集重产出

1. fixtures 级：`pnpm test`（dev e2e + prod e2e）自动重截 `docs/screenshots/`（dev-* / prod-* 系列，入库）。
2. testbed 级：dev + prod(8662) 每功能截图 `docs/screenshots/tb-*-*.png`（.gitignore 排除，**本地必须产出**，manual.html 引用它们）。
3. manual.html 引用的每一张截图逐一点开核对新品牌产物名（fulgur-remoteEntry.js 等）出现在截图 DOM/网络面板中；旧品牌截图一律被新图覆盖，不得残留。

## 8. 阶段七：文档核对 + 分阶段提交

1. 改动代码/配置同步核对注释与文档（"手册/迁移指南/DESIGN/交接清单"里 grep `fulgur-` 关键产物名与端口，与实现对上）。
2. 按阶段 git 本地提交（目录改名与路径同步、e2e 清理、fixtures 清理、docs 清理、testbed 重建基线、站点 conf 托管各一笔，信息写明范围）。
3. **不 push**。输出总结报告：各阶段验证结果（单测数、e2e 通过数、27 页双环境结果）、截图清单、后台失联影响项、遗留问题。

## 9. 总验收清单

- [ ] 全仓库大小写不敏感 `unifed` = 0 命中（仅本任务书与 git 历史除外）
- [ ] 本地目录名 = `fulgur-federation`，旧目录名 0 引用
- [ ] 单测 97/97、fixtures dev e2e 10/10、prod e2e 8/8、H7 装后实测绿
- [ ] 8662 站点重建（30-fulgur-test-8662.conf + www/fulgur-test），curl 冒烟全过
- [ ] testbed 27 页 dev + prod 双环境回归，console 零报错
- [ ] 截图全集重产出且无旧品牌残留
- [ ] 全部本地提交，未 push，总结报告已输出
