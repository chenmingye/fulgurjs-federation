# 27 页双环境四格矩阵（迁移阶段 1 归档，2026-09-14）

> 标准：`docs/待办问题与迁移方案.md` §2.5（截图即测试）。四格 = 迁移前乾坤基线（8661，
> `docs/screenshots/baseline-8661/`，本地不入库）× 迁移后 dev(8773)/prod(8662)。
> 脚本：`e2e/matrix-shot.mjs`（数据驱动，登录→接口取真实参数→逐页直达+截图+断言）。
> 结果 JSON：`migration1-dev/matrix-{dev,prod}.json`；截图 `migration1-dev/m-{tag}-{no}-{name}.png`。

- 后台：本地 demo 原项目（localhost:8085/demo，admin/Demo@123456）；27 页与基线逐条同源可比。
- 断言：路由可达 + 关键文本 + 零 pageerror + iframe=0；渲染状态 ok=页面文本>200 字符。

| # | 页面 | 迁移前基线(8661) | dev(8773) | prod(8662) | 说明 |
|---|------|-----------------|-----------|------------|------|
| 01 | 待办任务 | 01-bpmTask_flowable_bpm_task_todo.png | ✅ ok（1952） | ✅ ok（1952） |  |
| 02 | 已办任务 | 02-bpmTask_flowable_bpm_task_done.png | ✅ ok（2390） | ✅ ok（2390） |  |
| 03 | 我的流程 | 03-bpmTask_flowable_bpm_task_my.png | ✅ ok（1133） | ✅ ok（1133） |  |
| 04 | 抄送我的 | 04-bpmTask_flowable_bpm_task_copy.png | ✅ ok（232） | ✅ ok（232） |  |
| 05 | 发起流程 | 05-bpmTask_flowable_bpm_task_create.png | ✅ ok（383） | ✅ ok（383） |  |
| 06 | 流程详情 | 06-.../process-instance_detail.png | ✅ ok（349） | ✅ ok（384） |  |
| 07 | 审批操作(发起表单) | 07-.../manager_action.png | ✅ ok（208） | ✅ ok（208） |  |
| 08 | 流程模型 | 08-baseConfig_manager_flowable_bpm_manager_model.png | ✅ ok（255） | ✅ ok（255） |  |
| 09 | 流程表单 | 09-.../manager_form.png | ✅ ok（538） | ✅ ok（538） |  |
| 10 | 流程分类 | 10-.../manager_category.png | ✅ ok（543） | ✅ ok（543） |  |
| 11 | 用户分组 | 11-.../manager_user-group.png | ⚠️ ok（384） | ⚠️ ok（384） | 断言文本误报：页面标题为“用户分组”，渲染正常（截图为证）；missing dev=['用户组'] prod=['用户组'] |
| 12 | 流程监听器 | 12-.../manager_process-listener.png | ✅ ok（438） | ✅ ok（438） |  |
| 13 | 流程表达式 | 13-.../manager_process-expression.png | ✅ ok（1047） | ✅ ok（1047） |  |
| 14 | 流程实例管理 | 14-.../manager_process-instance_manager.png | ✅ ok（1632） | ✅ ok（1632） |  |
| 15 | 任务管理 | 15-.../manager_process-tasnk.png | ✅ ok（2129） | ✅ ok（2129） |  |
| 16 | 创建流程 | 16-.../manager_model_create.png | ✅ ok（271） | ✅ ok（271） |  |
| 17 | 表单设计 | 17-.../manager_form_edit.png | ✅ ok（606） | ✅ ok（606） |  |
| 18 | 修改流程 | 18-.../manager_model_update_@id.png | ✅ ok（278） | ✅ ok（278） |  |
| 19 | 复制流程 | 19-.../manager_model_copy_@id.png | ✅ ok（274） | ✅ ok（274） |  |
| 20 | 流程定义 | 20-.../manager_definition.png | ✅ ok（1993） | ✅ ok（1993） |  |
| 21 | 流程报表 | 21-.../process-instance_report.png | ✅ ok（973） | ✅ ok（973） |  |
| 22 | 表单设计(lowcode) | 22-online_lowcode_lowdev_formDesign.png | 🟡 路由可达/空白（173） | 🟡 路由可达/空白（173） | U-2 空白（lowcode 渲染层，路由+页签正常） |
| 23 | 报表设计(lowcode) | 23-..._reportDesign.png | 🟡 路由可达/空白（173） | 🟡 路由可达/空白（173） | U-2 空白（lowcode 渲染层，路由+页签正常） |
| 24 | 图形报表(lowcode) | 24-..._graphReportDesign.png | 🟡 路由可达/空白（173） | 🟡 路由可达/空白（173） | U-2 空白（lowcode 渲染层，路由+页签正常） |
| 25 | 模块设计(lowcode) | 25-..._moduleDesign.png | 🟡 路由可达/空白（174） | 🟡 路由可达/空白（174） | U-2 空白（lowcode 渲染层，路由+页签正常） |
| 26 | 报表功能测试 | 26-..._reportTest_@code.png | 🟡 路由可达/空白（175） | 🟡 路由可达/空白（175） | U-2 空白（lowcode 渲染层，路由+页签正常） |
| 27 | 外部表单 | 27-..._form_external_@type_@id.png | 🟡 路由可达/空白（168） | 🟡 路由可达/空白（168） | U-2 空白（lowcode 渲染层，路由+页签正常） |

## 汇总

- **bpm 21 页（01-21）：dev/prod 双环境全部渲染正常、iframe=0、零 pageerror**；
- **lowcode 6 页（22-27）：路由+页签全部可达（深链接直达），渲染层空白 = U-2**
  （`docs/UNRESOLVED.md`：avue UMD 构建限制，候选方向已记录）；
- 断言差异仅 11-user-group 一处文本误报（“用户分组” vs 断言“用户组”），双环境一致；
- 与 8661 基线逐条同源可比（同一 demo 后台、同一菜单结构）。

---

## 品牌清理收尾复测（2026-09-16 终版）

> 触发本轮复测的两项修复：① lowcode dayjs→esm 别名改为 **dev 专用**（build 不挂——
> prod rollup 下该别名把 element-plus 的 CJS require 导向 ESM 双重 interop →
> `p.default.extend` TypeError，历史结论 d3af5de 重演；而 dev 下 element-plus 被移出
> 预构建后其 dayjs/plugin CJS 子路径裸导入无 interop，别名又不可少——最终形态：
> `command==='serve'` 才注入别名，optimize exclude 维持含 dayjs）；
> ② bpm 详情页模板补回联邦挂载点（重建时丢失 `<component :is>`，AMIS/CUSTOM 业务表单区
> 空转空 src iframe——现恢复 loadRemote 直渲染 + iframe 兜底）。
> 终版产物：lowcode/bpm/admin prod 全量重构建并部署 8662；dev 三服务同配置重启。

| 维度 | dev (8773) | prod (8662) |
|---|---|---|
| 27 页 pass | 26/27（仅 11 文本误报，同历史） | 26/27（仅 11 文本误报，同历史） |
| pageErrors | **全部 0**（含 25） | **全部 0**（含 25） |
| 控制台报错（collect 全量） | **0 条** | **0 条** |
| iframe | **全部 0** | **全部 0** |
| `p.default.extend` | 无 | **已消灭**（build 别名已移除） |
| 06 流程详情 | textLen 356~376、iframe=0、零错（联邦直渲染） | textLen 376、iframe=0、零错 |
| 22~27 lowcode | 路由+页签可达，渲染空白 = U-2（avue UMD dev 限制），零报错 | 同左，零报错 |
| 25-moduleDesign | 空态渲染（诚实空垫片+形状守卫），零报错 | 同左，零报错 |

**25-moduleDesign 报错误已彻底修复（2026-09-16 深夜追平）**：
此前 2 条报错 = 本地 demo 后台无 `POST /demo/tab/page`（带鉴权 404）+ `graphreport/page`
异常形状 + 原项目 `.map` 无防护（与 SVN 只读原件逐字一致）。本轮按 2026-09-15 H1 先例重建：
① `moduleDesign/index.vue` 三处字典 `.map` 加形状守卫 + `getTableData` 显式 catch（失败
console.warn 可见、不静默吞）；② 诚实空垫片——dev = admin vite 中间件
（`fulgur:tab-page-empty-shim`，代理前拦截直出空页 jeecg Result），prod = NGINX
`location = /demo/tab/page`（live conf 与仓库托管副本同步）。页面渲染"暂无数据"，双环境
控制台报错归零，未伪造任何业务数据。

**同轮插件能力落地（brand cleanup 追加）**：① D.1 dev 守卫——exposes 目标文件静态导入
`virtual:fulgur-runtime` 显式报错（此前该误用静默破坏渲染上下文，排障 40min+）；
② D.3 runtime 官方化——`getRuntime()` / `version` / 方法面冻结；③ bpm dayjs 别名对齐
dev 专用（与 lowcode 终解一致，prod 构建彻底去别名）。验证：单测 106/106、fixtures
dev 10/10 + prod 8/8、H7 七项、runtime gzip 5034 ≤ 5120 红线。

配套复验（同为终版产物，含新插件 dist 重构建的三应用）：collect-errors dev **0 条** +
prod **0 条**（27 页全量控制台报错双零，含 25）；flow-closure dev 5/5 + prod 5/5；
pending-routes 12 页双环境全 pass（iframe=0）；amis 详情联邦直渲染 dev/prod
iframe=0、标题+字段渲染、零错误（t5-*-02-amis-detail 截图重产）；
fulgur-demo prod 双远程组件+交互 20% 零错误；8662 最小宿主页
（`e2e/nginx/minimal-host.html`，新 runtime 含 getRuntime/version）双远程组件 +
vue/pinia 双版本协商零错误；跨应用 HMR（改 bpm 源码 → admin 页面热替换）70% 状态保留零错误。

---

## 附2：全新重建 testbed 终验（2026-09-17 凌晨，D.2/D.5 落地后）

> 用户删除旧 testbed 后按指示从 svn 原项目全新拷贝重建（联邦集成全量重写），并落地
> D.2 defineFulgurPages / D.5 错误诊断体系 / D.4 第一步（EP 进 lowcode shared）。
> 构建期曾因 admin .env.production 缺 VITE_BUILD_COMPRESS 崩溃（交接文档已知项，已补 none）。

| 维度 | dev (8773) | prod (8662) |
|---|---|---|
| 27 页矩阵 | 路由级全部可达渲染，iframe 除 06=1 外全零，pageErrors 仅 07 一条 | pageErrors 全零，iframe 仅 06=1，fail 仅 11 误报 |
| collect 控制台报错 | 0 条 | 1 条（25 页 getBoundingClientRect，EP shared 新引入待查） |
| flow 全链路闭环 | 5/5 ✓ | 5/5 ✓ |
| amis 验证 | 行点击 ✓（详情字段渲染待查，见已知问题 1） | 同左 |
| fulgur-demo | 双远程组件 + 交互 20% ✓ 零错 | — |
| 待办列表行渲染 | 10 行（含 AMIS 行）✓ | 10 行 ✓ |

### 已知问题（本次重建暴露，待专项处理）
1. **06 流程详情 AMIS 表单区回落 iframe 通道**（iframe=1，经 /main/AmisFormRouterPage
   iframe 展示、字段渲染待查；dev 07 另有 'name' undefined pageerror）。诊断：detail 页
   isFederatedRealm 判定时序与「staticRouter 直挂路由 + LayoutContent 联邦分支」双通道
   并存的现状需重查触发条件。
2. **lowcode 产物 chunk 重复未收敛**（2×9.87MB worker、2×5.57MB index；EP 进 shared
   收敛了运行时实例但未收敛构建切分）→ D.4 专项继续。
3. matrix 的 textLen>200 判据会被侧栏文本掩盖空内容——后续应为菜单页增加
   「表格行数>0 或明确空态」断言（本次已用 todo-rows/截图人工复核补位）。

### 本轮修复（重建过程中即修，全部已验证）
- bpm `/redirect` 父子同名 'Redirect'：shared 协商升 vue-router 4.6.4 后强制唯一名，
  子路由改名 RedirectChild（原项目代码与新 vue-router 的兼容问题）
- bpm federatedBoot 重写：最小状态注入（permissions 通配 Set + setDictMap），
  替代会级联构建路由表的 setUserInfoAction（根因：联邦下与宿主路由撞名）
- admin permission.ts：/flowable/**、/lowcode/** 后台空路由剔除（联邦通道承载），
  修复「后台无组件路由抢先匹配 → 页面空白 + Redirect 级联」
- admin LayoutContent：补联邦分支（fulgur 路径直渲染组件 + params/query 透传 +
  fullPath key），修复内容区渲染空乾坤容器
- admin main.ts：乾坤注册门控（QIANKUN_OPEN=false 不启动 single-spa）
- admin .env.production：补 VITE_BUILD_COMPRESS=none（构建崩溃修复，交接文档已知项）
- admin fulgurBridge：权限码读取修正（getPermCodeList 为 state 数组非方法）
## 附：22 表单设计性能实测 与 分页国际化已知问题（2026-09-16 深夜）

### 性能实测（菜单点击 → 表格行出现，e2e/perf-formdesign-probe.mjs，本机 localhost）

| 指标 | 联邦 8662 | 乾坤基线 8661 |
|---|---|---|
| 冷加载（首次点菜单） | **4.45s** | **5.02s** |
| 冷加载 /lowcode/ 传输量 | **3.6MB**（84 请求，解码 13.8MB） | **17.4MB**（60 请求，解码 18.6MB） |
| 热加载（二次点菜单） | **3.5~3.9s** | **4.4~4.5s** |
| 热加载网络请求 | 0（全缓存） | 0（全缓存） |

结论：**联邦不慢于乾坤**（冷/热均更快、传输量约 1/5）；表单设计页冷热只差 ~1s，
说明 ~3.5s 的大头是页面自身的 JS 执行/渲染（在线表单设计器：设计器+monaco+avue 栈，
两架构同款成本），并非联邦加载开销。另注意 admin 登录后整壳稳定需 20s+（后台
dept/simple-list 等 13s 级慢链，乾坤时代同款），对"感觉慢"的体感贡献显著。

### 分页国际化已知问题（element-plus 文案显示英文，如分页 "Total"）

**现象**：lowcode 6 页在联邦 prod 下 el-pagination 等文案为英文（乾坤时代为中文）。

**根因（已定位，待设计解决）**：乾坤时代 lowcode 单应用自带一份 element-plus 副本 +
App.vue 的 ConfigGlobal（ElConfigProvider）注入中文；联邦 lowcode 产物内存在**多份
element-plus 副本**（见 D.4），provide/inject 键按副本隔离，avue-crud 内部分页组件的
inject 无法命中任何 app 级 provide（宿主侧/远程侧三处 provide 均实测落上宿主 app
context 且值正确，仍英文）。修复依赖「副本收敛」（评估文档 D.4），单独的 locale 注入
无法跨副本生效。

---

## 附3：遗留项清理终验（2026-09-17，闲时任务 A→G 全清）

> 执行文档 `docs/闲时任务-遗留项清理-2026-09-17.md`。本轮 A~F 全部落地并双环境验证，
> 附2 三条已知问题全部闭案（1 修复 / 2 定论 / 3 落地）。

### 逐项结论

| 项 | 结论 | 验收 |
|---|---|---|
| A 06-AMIS iframe 回归 | 第三次重拷丢联邦挂载点（svn 原版直出）+ bpm remotes 误用 `name@url` 对象形式（该语法仅字符串 external 支持，整串被当 URL 拼出非法 specifier）。修：detail 双分支恢复 `loadRemote` 直渲染（`<component :is>` + `:form-params` + iframe 兜底，realm 判定改实时读 `__FULGUR_RUNTIME__`）；remotes 改裸 URL | dev+prod amis 验证 iframe=0、AMIS 标题+字段直渲染、零错误；dev 矩阵 06 iframe=0 |
| B dev-07 pageerror | expose 误挂**子组件** ProcessDefinitionDetail（需父页传 `selectProcessDefinition` prop），原路由实为**独立页** ProcessDefinitionDetailMaster（自带 processsKey 拉取）。prod 旧"无错"只是 Vue 生产版 render 错误走 console 不 rethrow，同为空壳。修：expose 改指 Master | dev 07 零 pageerror/console.error、textLen 205（流程：请个假审批流程+时间线+按钮）；prod 随全量回归复验 |
| C prod-25 getBoundingClientRect | EP 2.10.2 `use-content.mjs:50` watch getter 无守卫调用 `reference.getBoundingClientRect()`；页面自身两处 el-tooltip 未挂载非责任方；EP shared 已知限制 | 记 UNRESOLVED U-6（含规避：升级 EP / pnpm patch 守卫）；pageerror 恒 0、功能无损 |
| D 分页国际化 | **跨应用双 EP 副本**是根因：avue 内部 el-pagination 经宿主全局组件注册表解析到 **admin 的 EP 副本**（Symbol 隔离），EP shared 只收敛了 lowcode 产物内部副本。修：双侧注入——lowcode federatedBoot `provideGlobalConfig({locale: zhCn}, hostApp)`（**深路径导入**绕开 U-7 坏门面）+ admin fulgurBridge 用宿主副本同款注入 | dev+prod formDesign 分页「共 88 条 / 10条/页 / 前往页」全中文 ✓ |
| E D.4 chunk 重复 | **重复 = 跨构建残留**（outDir 在项目根外 vite 默认不清空，旧 hash 累积曾被误判为构建期重复）；单次构建模块图唯一（诊断插件 dump 6959 id 验证，permission×8 实为 5 次构建残留+2 个同名源文件）。修：三应用 `emptyOutDir: true`。干净基线：lowcode 514 js / assets 29MB（最大 index 5.6MB 单份）；monaco 双目录（18M+17M）为 monacoEditorPlugin publicPath/base 双写，**原项目固有行为不动**。附带定位插件裸门面 export*+TLA 缺陷 → UNRESOLVED U-7 | 干净构建+部署；27 页双环境全绿不回归 |
| F matrix 判据 | menu 页新增「可见表格行/卡片 > 0 或表格自身空元素」断言 + 30s 内容就绪轮询（防侧栏文本掩盖空白、防加载中瞬时空态误判）；11 断言文本修正为页面真实标题「用户分组」 | dev 27/27 全 PASS（08 模型页从"255 假绿"修正为 76 行真绿） |

### 本轮产物与文件变更

- bpm：`detail/index.vue`（联邦直渲染恢复）、`vite.config.ts`（remotes 修正 + action expose 改 Master + emptyOutDir）
- lowcode：`federatedBoot.ts`（EP locale 注入，深路径）、`vite.config.ts`（emptyOutDir；诊断插件已移除）
- admin：`fulgurBridge.ts`（宿主 EP locale 注入）、`vite.config.ts`（emptyOutDir）
- e2e：`matrix-shot.mjs`（F 判据）；新增 diag06b/diag07/diag25*/diag-pagination/diag-f* 系列诊断探针（入库备查）
- prod 重构建部署：admin + bpm + lowcode 全量 rsync 8662（截图 54 张随矩阵重产）
