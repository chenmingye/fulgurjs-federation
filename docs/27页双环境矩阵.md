# 27 页双环境四格矩阵（迁移阶段 1 归档，2026-09-14）

> 标准：`docs/待办问题与迁移方案.md` §2.5（截图即测试）。四格 = 迁移前乾坤基线（8661，
> `docs/screenshots/baseline-8661/`，本地不入库）× 迁移后 dev(8773)/prod(8662)。
> 脚本：`e2e/matrix-shot.mjs`（数据驱动，登录→接口取真实参数→逐页直达+截图+断言）。
> 结果 JSON：`migration1-dev/matrix-{dev,prod}.json`；截图 `migration1-dev/m-{tag}-{no}-{name}.png`。

- 后台：本地 demo 原项目（localhost:8085/demo，admin/Demo@123456）；27 页与基线逐条同源可比。
- 断言：路由可达 + 关键文本 + 零 pageerror + iframe=0；渲染状态 ok=页面文本>200 字符。

| # | 页面 | 迁移前基线(8661) | dev(8773) | prod(8662) | 说明 |
|---|------|-----------------|-----------|------------|------|
| 01 | 待办任务 | 01-bpmTask_flowable_bpm_task_todo.png | ✅ ok（1952） | ✅ ok（1952） |  |
| 02 | 已办任务 | 02-bpmTask_flowable_bpm_task_done.png | ✅ ok（2390） | ✅ ok（2390） |  |
| 03 | 我的流程 | 03-bpmTask_flowable_bpm_task_my.png | ✅ ok（1133） | ✅ ok（1133） |  |
| 04 | 抄送我的 | 04-bpmTask_flowable_bpm_task_copy.png | ✅ ok（232） | ✅ ok（232） |  |
| 05 | 发起流程 | 05-bpmTask_flowable_bpm_task_create.png | ✅ ok（383） | ✅ ok（383） |  |
| 06 | 流程详情 | 06-.../process-instance_detail.png | ✅ ok（349） | ✅ ok（384） |  |
| 07 | 审批操作(发起表单) | 07-.../manager_action.png | ✅ ok（208） | ✅ ok（208） |  |
| 08 | 流程模型 | 08-baseConfig_manager_flowable_bpm_manager_model.png | ✅ ok（255） | ✅ ok（255） |  |
| 09 | 流程表单 | 09-.../manager_form.png | ✅ ok（538） | ✅ ok（538） |  |
| 10 | 流程分类 | 10-.../manager_category.png | ✅ ok（543） | ✅ ok（543） |  |
| 11 | 用户分组 | 11-.../manager_user-group.png | ⚠️ ok（384） | ⚠️ ok（384） | 断言文本误报：页面标题为“用户分组”，渲染正常（截图为证）；missing dev=['用户组'] prod=['用户组'] |
| 12 | 流程监听器 | 12-.../manager_process-listener.png | ✅ ok（438） | ✅ ok（438） |  |
| 13 | 流程表达式 | 13-.../manager_process-expression.png | ✅ ok（1047） | ✅ ok（1047） |  |
| 14 | 流程实例管理 | 14-.../manager_process-instance_manager.png | ✅ ok（1632） | ✅ ok（1632） |  |
| 15 | 任务管理 | 15-.../manager_process-tasnk.png | ✅ ok（2129） | ✅ ok（2129） |  |
| 16 | 创建流程 | 16-.../manager_model_create.png | ✅ ok（271） | ✅ ok（271） |  |
| 17 | 表单设计 | 17-.../manager_form_edit.png | ✅ ok（606） | ✅ ok（606） |  |
| 18 | 修改流程 | 18-.../manager_model_update_@id.png | ✅ ok（278） | ✅ ok（278） |  |
| 19 | 复制流程 | 19-.../manager_model_copy_@id.png | ✅ ok（274） | ✅ ok（274） |  |
| 20 | 流程定义 | 20-.../manager_definition.png | ✅ ok（1993） | ✅ ok（1993） |  |
| 21 | 流程报表 | 21-.../process-instance_report.png | ✅ ok（973） | ✅ ok（973） |  |
| 22 | 表单设计(lowcode) | 22-online_lowcode_lowdev_formDesign.png | 🟡 路由可达/空白（173） | 🟡 路由可达/空白（173） | U-2 空白（lowcode 渲染层，路由+页签正常） |
| 23 | 报表设计(lowcode) | 23-..._reportDesign.png | 🟡 路由可达/空白（173） | 🟡 路由可达/空白（173） | U-2 空白（lowcode 渲染层，路由+页签正常） |
| 24 | 图形报表(lowcode) | 24-..._graphReportDesign.png | 🟡 路由可达/空白（173） | 🟡 路由可达/空白（173） | U-2 空白（lowcode 渲染层，路由+页签正常） |
| 25 | 模块设计(lowcode) | 25-..._moduleDesign.png | 🟡 路由可达/空白（174） | 🟡 路由可达/空白（174） | U-2 空白（lowcode 渲染层，路由+页签正常） |
| 26 | 报表功能测试 | 26-..._reportTest_@code.png | 🟡 路由可达/空白（175） | 🟡 路由可达/空白（175） | U-2 空白（lowcode 渲染层，路由+页签正常） |
| 27 | 外部表单 | 27-..._form_external_@type_@id.png | 🟡 路由可达/空白（168） | 🟡 路由可达/空白（168） | U-2 空白（lowcode 渲染层，路由+页签正常） |

## 汇总

- **bpm 21 页（01-21）：dev/prod 双环境全部渲染正常、iframe=0、零 pageerror**；
- **lowcode 6 页（22-27）：路由+页签全部可达（深链接直达），渲染层空白 = U-2**
  （`docs/UNRESOLVED.md`：avue UMD 构建限制，候选方向已记录）；
- 断言差异仅 11-user-group 一处文本误报（“用户分组” vs 断言“用户组”），双环境一致；
- 与 8661 基线逐条同源可比（同一 demo 后台、同一菜单结构）。

---

## 品牌清理收尾复测（2026-09-16 终版）

> 触发本轮复测的两项修复：① lowcode dayjs→esm 别名改为 **dev 专用**（build 不挂——
> prod rollup 下该别名把 element-plus 的 CJS require 导向 ESM 双重 interop →
> `p.default.extend` TypeError，历史结论 d3af5de 重演；而 dev 下 element-plus 被移出
> 预构建后其 dayjs/plugin CJS 子路径裸导入无 interop，别名又不可少——最终形态：
> `command==='serve'` 才注入别名，optimize exclude 维持含 dayjs）；
> ② bpm 详情页模板补回联邦挂载点（重建时丢失 `<component :is>`，AMIS/CUSTOM 业务表单区
> 空转空 src iframe——现恢复 loadRemote 直渲染 + iframe 兜底）。
> 终版产物：lowcode/bpm/admin prod 全量重构建并部署 8662；dev 三服务同配置重启。

| 维度 | dev (8773) | prod (8662) |
|---|---|---|
| 27 页 pass | 26/27（仅 11 文本误报，同历史） | 26/27（仅 11 文本误报，同历史） |
| pageErrors | **全部 0**（含 25） | **全部 0**（含 25） |
| 控制台报错（collect 全量） | **0 条** | **0 条** |
| iframe | **全部 0** | **全部 0** |
| `p.default.extend` | 无 | **已消灭**（build 别名已移除） |
| 06 流程详情 | textLen 356~376、iframe=0、零错（联邦直渲染） | textLen 376、iframe=0、零错 |
| 22~27 lowcode | 路由+页签可达，渲染空白 = U-2（avue UMD dev 限制），零报错 | 同左，零报错 |
| 25-moduleDesign | 空态渲染（诚实空垫片+形状守卫），零报错 | 同左，零报错 |

**25-moduleDesign 报错误已彻底修复（2026-09-16 深夜追平）**：
此前 2 条报错 = 本地 demo 后台无 `POST /demo/tab/page`（带鉴权 404）+ `graphreport/page`
异常形状 + 原项目 `.map` 无防护（与 SVN 只读原件逐字一致）。本轮按 2026-09-15 H1 先例重建：
① `moduleDesign/index.vue` 三处字典 `.map` 加形状守卫 + `getTableData` 显式 catch（失败
console.warn 可见、不静默吞）；② 诚实空垫片——dev = admin vite 中间件
（`fulgur:tab-page-empty-shim`，代理前拦截直出空页 jeecg Result），prod = NGINX
`location = /demo/tab/page`（live conf 与仓库托管副本同步）。页面渲染"暂无数据"，双环境
控制台报错归零，未伪造任何业务数据。

**同轮插件能力落地（brand cleanup 追加）**：① D.1 dev 守卫——exposes 目标文件静态导入
`virtual:fulgur-runtime` 显式报错（此前该误用静默破坏渲染上下文，排障 40min+）；
② D.3 runtime 官方化——`getRuntime()` / `version` / 方法面冻结；③ bpm dayjs 别名对齐
dev 专用（与 lowcode 终解一致，prod 构建彻底去别名）。验证：单测 106/106、fixtures
dev 10/10 + prod 8/8、H7 七项、runtime gzip 5034 ≤ 5120 红线。

配套复验（同为终版产物，含新插件 dist 重构建的三应用）：collect-errors dev **0 条** +
prod **0 条**（27 页全量控制台报错双零，含 25）；flow-closure dev 5/5 + prod 5/5；
pending-routes 12 页双环境全 pass（iframe=0）；amis 详情联邦直渲染 dev/prod
iframe=0、标题+字段渲染、零错误（t5-*-02-amis-detail 截图重产）；
fulgur-demo prod 双远程组件+交互 20% 零错误；8662 最小宿主页
（`e2e/nginx/minimal-host.html`，新 runtime 含 getRuntime/version）双远程组件 +
vue/pinia 双版本协商零错误；跨应用 HMR（改 bpm 源码 → admin 页面热替换）70% 状态保留零错误。
