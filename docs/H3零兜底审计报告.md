# H3 零兜底审计报告

> 日期：2026-09-15（迁移阶段 3 / 路线图 P0-3）
> 立场依据：`docs/插件成型路线图.md` §0——「兜底路径 = 缺陷掩护器」，联邦不可用必须显式报错。
> 静态检查：`bash e2e/h3-audit.sh`（4 项检查全部通过，可随时重跑回归）

## 一、本次删除的兜底/降级路径（4 处）

| # | 位置 | 原行为 | 现行为 |
|---|------|--------|--------|
| 1 | `demo-bpm/src/views/bpm/processInstance/detail/index.vue`（CUSTOM 业务表单） | 联邦装载失败 → 回落 iframe 通道（嵌宿主 FormRouterPage + query 参数） | 显式 `throw new Error('[federation] 业务表单联邦组件加载失败…')`，附可执行修复建议 |
| 2 | 同上（AMIS 表单） | 联邦装载失败 → 回落 iframe 通道（AmisFormRouterPage） | 同上显式抛错 |
| 3 | `demo-lowcode/src/fulgur-exposes/federatedBoot.ts` | 分段容错：全局注册（globCom/lowDesagn/avue/auth）单项失败仅 console.warn「忽略」 | 任一步失败即抛错（附步骤名），不让页面带着缺失的全局注册静默运行 |
| 4 | `demo-host/src/qiankun/fulgurPages.ts` `bootRemoteOnce` | federatedBoot 加载/初始化失败仅 warn「跳过」，页面照常渲染 | 显式抛错（含 MFU-006 语义与配置检查建议） |

随删死代码：`FormComponentUrl`/`AmisFormComponentUrl` refs、`toQueryString`、iframe 专用 queryParams 拼装。
`useFederatedPage`（federatedForm.ts）语义同步更新：load 返回 false 时调用方必须显式报错。

## 二、合理保留清单（非联邦失败兜底，逐项说明）

| 位置 | 性质 | 保留理由 |
|------|------|---------|
| `admin/.../JeecgPdfView.vue` | PDF 预览 iframe | 原版功能：PDF 只能以 iframe/内嵌对象呈现，与联邦无关（路线图 §4 P1-1 认可） |
| `admin/sys/iframe/index.vue`、`bpm|lowcode/components/IFrame` | 外链通用容器 | 原版功能：菜单外链页面容器 |
| `admin/.../portalapp/.../newsCord.vue` | 门户新闻 iframe | 原版功能：新闻内容外链渲染 |
| `admin/system/notice/DetailModal.vue` | 通知详情 iframe | 原版功能：通知富文本/外链展示 |
| `admin/super/airag/.../AiAppSendModal.vue` | AI 应用生成 iframe 片段 | 原版功能：AI 应用预览，字符串拼接 HTML |
| `admin/.../FormDetailsModal.vue` 等 6 处跨应用弹窗 iframe | 乾坤时代跨应用展示 | **非兜底**，是待联邦化的存量功能 → 路线图 P1-1 专项处理 |
| `admin/src/qiankun/apps.ts` 两处 `catch {}` | URL 解析容错 | 输入规范化（非法 URL 按原文返回），与联邦可用性无关；已补注释声明（H3 审计注） |
| `admin/.../AmisFormRouterPage.vue` formParams→route.query 回落 | 参数来源兼容 | 组件内部参数解析顺序（props 优先），联邦失败时不会走到这里；iframe 用法兼容层，阶段 4 乾坤下线时随之下线 |
| `admin/utils/ssoLogin.ts`、条码打印 null→''、业务「回退」按钮等 | 登录/打印/业务动作 | 与联邦无关的原版业务逻辑 |

## 三、删后验证（证据）

1. **核心链路可用**（e2e/h3-detail-verify.mjs，dev 第二轮）：待办任务 → 办理 → 流程详情页
   （AMIS 联邦直渲染路径）：form-box 渲染、时间线/印章正常、**iframe=0、pageerror=0、失败请求=0**。
2. **故障注入显式报错**：杀掉 bpm dev server（remote 不可达）后访问待办任务页 →
   表格 0 行、无任何静默内容，显式报错
   `FulgurError: [fulgur:MFU-001] failed to load remote "mes-bpm" … 1. is the remote dev server running? …`
   （三段式错误码+可执行建议，H9 语义）。
3. **静态检查**：`bash e2e/h3-audit.sh` 退出码 0（4 项 ✓）。

## 四、遗留与后续

- 6 处跨应用弹窗 iframe 属 P1-1（联邦化改造，非兜底删除）。
- `admin/src/qiankun/` 整个乾坤兼容层在阶段 4（乾坤下线）时一并移除，届时 apps.ts 的 URL 容错随之消失。
