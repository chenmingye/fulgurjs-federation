# 品牌清理续作交接（2026-09-16 傍晚快照）

> 前序：`docs/品牌清理与全量回归任务书.md`（总纲）。本文档记录该任务书的执行进度快照：
> 阶段一~四已全部完成并提交，阶段五 dev/prod 双环境功能验证已全绿，**剩余收尾工作按下文清单执行**。
> 下一轮执行者从「三、未完成清单」开始，按 A→G 顺序做。

---

## 一、当前状态快照（全部已实测，非推断）

### 任务书阶段进度

| 阶段 | 状态 | 证据 |
|---|---|---|
| 一 目录改名+路径同步 | ✅ 完成（commit f68fedc） | 全仓 grep 旧路径=0（任务书自身除外） |
| 二 代码配置清理 | ✅ 完成（22af592 / 261add1 / 4a22df6） | 大小写不敏感 grep unifed 仅剩任务书 |
| 三 单测+fixtures 回归 | ✅ 全绿 | 单测 **97/97**、dev e2e **10/10**、prod e2e **8/8**、H7 装后实测七项断言全过（含补上的 LICENSE） |
| 四 NGINX 8662 重建 | ✅ 完成（c168a82） | curl 冒烟：200/SPA回退/immutable/CORS/gzip/反代 401 鉴权全过 |
| 五 testbed 重建+27页双环境 | 🔶 功能验证全绿，收尾见下文 | 见下表 |
| 六 截图证据全集 | 🔶 矩阵级已产出，manual 引用核对未做 | 见未完成 B |
| 七 文档核对+分阶段提交 | ❌ 未开始 | 见未完成 F |

### 阶段五 testbed 实测结果（2026-09-16）

| 验证项 | dev (8773) | prod (8662) |
|---|---|---|
| 27 页矩阵（matrix-shot.mjs） | **27/27 pass** | **26/27 pass**（11-user-group 断言误报，历史已知；06 iframe=1 待确认，见 C.2） |
| 全链路闭环（flow-closure.mjs） | 5/5 ✓ | 5/5 ✓ |
| CRUD 写操作（func-suite.mjs 09/10/11/12/13） | 全 ✓ | 全 ✓ |
| 动作闭环（model-actions.mjs 八动作） | 8/8 ✓，pageerror=0 | 8/8 ✓，pageerror=0 |
| fulgur-demo 演示页 | 双远程组件渲染+交互 20% ✓ | 未跑（tb-demo-shot.mjs 带 VBASE 即可） |
| 三应用 prod 构建 | — | ✅ main 2m20s / flowable / lowcode，已部署 8662 |

- 结果 JSON：`docs/func-results/{dev,prod}{,-flow}.json`、`docs/screenshots/migration1-dev/matrix-{dev,prod}.json`（目录 .gitignore 排除，本地已落盘）
- 结果截图：`docs/screenshots/migration1-dev/m-{dev,prod}-*.png` 27×2 张 + dev-*/prod-* 闭环系列
- testbed 联邦改造已在本轮**全部重写落地**（旧副本曾随 7.6G 一起被删）：admin 四件套（fulgurPages/fulgurBridge/staticRouter/LayoutContent/main 门控）+ bpm/lowcode exposes/federatedBoot/vite 接入 + FormRouterPage/AmisFormRouterPage formParams 通道 + model/form 直链兜底。testbed/ 不入库，重建方法即任务书 §6 + 本文「四、坑指引」。

### 环境事实（接手先核对）

- 后台：`localhost:8085`（本地 demo 原项目，**活着**，admin/Demo@123456）；localhost 仍失联
- dev 三服务：8773/4529/4669（本轮结束时**仍在运行**，接手若发现挂了按 `cd testbed/demo-app && pnpm start` 拉起）
- 8662 站点：conf = `e2e/nginx/30-fulgur-test-8662.conf`（已含深链回退宿主修复），部署源 `testbed/demo-app/dist/{main,flowable,lowcode}`
- admin prod 构建两处规避已落地（见四.3），重建时**不要回退**

## 二、本轮 git 提交清单（全部本地，未 push）

```
f68fedc 阶段一：目录改名+路径同步
22af592 阶段二：e2e/配置清理
261add1 阶段二：fixtures 孤儿目录删除
4a22df6 阶段二：docs 清理
（LICENSE 补交）H7 七项断言全绿
c168a82 阶段四：8662 conf 托管
c10c0a6 阶段五验证收尾：matrix 登录加固/conf 深链修复/双环境全量结果与截图
```

## 三、未完成清单（下一轮按序执行）

### A. prod lowcode 设计器页 TypeError（建议最先做）

prod 矩阵 22~27（lowcode 6 页）`pageErrors` 出现 `TypeError: p.default.extend is not a function`
（dev 同 6 页 pageErrors=[]，渲染空白但无报错；textLen 459~475 比 dev 的 380 更大）。
疑似 prod rollup 路径下 dayjs esm 别名 interop 或 element-plus `extend` 调用（`p.default.extend`
形态像 dayjs/plugin 工厂或 elementPlus 插件安装）。排查入口：
1. `demo-lowcode/vite.config.ts` 的 dayjs 别名在 prod（`command==='build'`）是否命中（loadEnv 的 mode 差异）
2. `src/plugins/avue`、`src/plugins/lowDesagn` 的 dayjs/element-plus 引用链在 prod chunk 里的 interop
3. 修复标准：prod 矩阵 22~27 `pageErrors: []`（保持 u2-blank 可接受，U-2 是 avue UMD 已知限制）
4. 重跑：`VBASE=http://localhost:8662 VTAG=prod node e2e/matrix-shot.mjs`

### B. 截图证据收尾（任务书阶段六）

1. **tb-* 系列归档**：`/tmp/tb-demo.png`、`/tmp/tb-demo-clicked.png`、`/tmp/tb-smoke.png`、`/tmp/tb-detail.png`
   → 拷入 `docs/screenshots/` 改名 `tb-dev-fulgur-demo.png`、`tb-dev-fulgur-demo-advanced.png`、
   `tb-dev-todo-federated.png`、`tb-dev-detail-timeline.png`
2. **manual.html 引用逐张核对**（任务书阶段六.3）：manual §5 引用的 `tb-dev-demo-page /
   tb-dev-remotes-loaded / tb-dev-scope-negotiation`、§5.1 `tb-prod-minimal-host`、§6
   `tb-dev-hmr-before / tb-dev-hmr-crossapp`、遗留2 `t5-dev-02-amis-detail / t5-prod-02-amis-detail`。
   缺的重拍（需要小脚本：scope 协商网络面板、prod 最小宿主 runtime API 页、bpm 源码改动→admin 页面
   HMR 热更）。旧品牌字样截图必须被新图覆盖。
3. **pending-routes-shot.mjs**（12 隐藏路由页）dev+prod 各重跑一遍归档
4. **amis-dev-verify.mjs** dev+prod（`VTAG=prod VBASE=http://localhost:8662`）重跑（遗留 2 复验：
   AMIS 表单区 iframe=0）
5. **collect-errors.mjs** 重跑刷新 `docs/func-results/{dev,prod}-console-errors.json`
6. fulgur-demo prod 一遍：`VBASE=http://localhost:8662 node e2e/tb-demo-shot.mjs`

### C. 两个待确认项

1. **prod 矩阵 06-detail iframe=1**：该实例是 AMIS 类型（截图显示已取消+审批意见完整渲染）。
   确认这个 iframe 是 amis 渲染器内部产物还是业务表单区回落了 iframe 通道（遗留 2 断言 iframe=0）。
   若是回落：查 prod 下 `window.mainAppProps` 写入时序 vs detail 模块求值时序。
2. **dev 矩阵 06 textLen=226**（历史基线 349，probe 实测时间线 3 节点完整渲染）：确认为冷转换等待
   差异即可，不必修。

### D. 插件改进项（本轮实测暴露的易用性问题，建议排期，非本轮必做）

1. **远程 SFC 静态导入 `virtual:fulgur-runtime` 的 dev 检测**：远程页面 SFC 静态导入该虚拟模块会
   破坏渲染上下文（resolveComponent/withDirectives/ref owner 全线告警、内容区静默空白，dev server
   无任何插件级报错）。插件可在 transform 阶段检测"远程应用源码 import 虚拟模块"并显式报错/自动改写
   为 globalThis 单例获取。本次排查耗掉约 40 分钟（二分定位）。
2. **带参路由 expose 键冲突告警**：宿主侧 spec 推导剥离 `:param` 后与列表页 expose 键收敛相同时
   （如 `model/:type/:id` → `pages/bpm/manager/model`），应告警提示用 spec 显式覆盖。
3. **`globalThis.__FULGUR_RUNTIME__` 官方化**：跨源单例目前是内部约定，建议提供
   `getRuntime()` 导出或在文档明确该契约（bpm 详情页消费 admin exposes 时必须走它）。

### E. git 收尾

1. 上述修复逐项提交（延续分阶段风格）
2. testbed/ 在 .gitignore 不入库（维持现状）；「testbed 重建基线」以本文档 + func-results + e2e
   脚本为基线载体

### F. 阶段七：文档核对 + 总结报告

1. grep 核对：`fulgur-remoteEntry.js`、`fulgur-manifest.json`、`8662/8773/4529/4669`、
   `fulgur-demo` 在 README / npm README / DESIGN.md / manual.html / 迁移指南 / 交接清单 与实现一致
2. DESIGN.md 已知差异 #7 补一笔：sourcemap off + TLA 插件停用 + es2022 组合是当前 testbed admin
   prod 构建的可行解（本轮实测）
3. 输出总结报告（任务书 §9 清单逐项勾选）→ **停下等用户，不 push**

### G. 总验收清单复核（任务书 §9）

- [x] 全仓库 unifed = 0（任务书与 git 历史除外）
- [x] 目录名 fulgur-federation
- [x] 单测 97/97、dev e2e 10/10、prod e2e 8/8、H7 绿
- [x] 8662 站点重建 + curl 冒烟
- [ ] testbed 27 页双环境 **console 零报错**（卡在 A 项：prod lowcode 6 页 TypeError）
- [ ] 截图全集重产出且无旧品牌残留（卡在 B 项）
- [ ] 全部本地提交 + 总结报告（F 项）

## 四、坑指引（本轮实测沉淀，接手必读）

1. **远程页面 SFC 禁止静态 `import 'virtual:fulgur-runtime'`**（bpm vite 5.1.4 实测复现）：
   渲染上下文破坏三件套告警 = resolveComponent / withDirectives / Missing ref owner；
   走 `globalThis.__FULGUR_RUNTIME__`。已用在 `bpm detail/index.vue`（有注释）。
2. **fulgurPages 带参条目必须 spec 显式覆盖**：`model/:type/:id` 已写
   `spec: 'pages/bpm/manager/model/update'`（有注释说明复制变更新的数据风险）。
3. **admin prod 构建三件套**（缺一即崩，均已落地勿回退）：
   `sourcemap: false`（swc OOM 16GB）+ 停用 `vite-plugin-top-level-await`（swc 1.15 AST
   "missing field type"）+ `build.target: 'es2022'`（联邦 TLA 要求）。
4. **8662 深链回退必须指向宿主** `/main/index.html`（子应用 index.html 无登录态会 401 弹回）。
5. **矩阵脚本登录**已加固为"等登录表单消失+重试 3 轮"（固定秒数在 prod 慢链必挂）。
6. 换插件 dist / 改 vite 配置后：`rm -rf` 三应用 `node_modules/.vite` + 重启三 dev server +
   全新浏览器 profile；bpm/lowcode 冷启动首轮 30s+ 假错误勿当回归。
7. 浏览器自动化一律 `NO_PROXY=localhost,127.0.0.1`（e2e 脚本已内置 --no-proxy-server）。

## 五、红线（继承任务书，全程有效）

demo-app 原项目只读、绝不 svn；git 只本地提交不 push；后台业务接口报错标注"环境问题"不伪造；
所有功能验证浏览器真实打开+截图留证；验证不绿必须停下排查。
