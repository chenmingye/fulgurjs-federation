# 本轮实施基线记录（2026-09-25）

> 冻结于实施开始前，供交付报告与回退对照。

## Git / SVN 状态

- 插件仓库 `fulgurjs-federation` @ master `9ba55ed`（docs：完成 4.1.0 MES-ZC 双环境验收）。本轮开始时工作区无未提交修改，仅 3 个未跟踪文件：
  - `docs/独立项目配置与4.1.0遗留问题实施任务书.md`（任务书本体）
  - `docs/screenshots/8662-bpm-approval-tabs-bottom-20260925.png`（用户提供的 8662 沉底截图证据）
  - `pnpm-lock.yaml`（用户根目录未跟踪文件，不改动）
- `testbed/mes-zc` 独立 SVN 工作副本：`svn status` 共 42 项（37 M + 5 ?），全部为 4.1.0 验收期既有改动，本轮继续在其上叠加，**不提交 SVN**。未跟踪目录：`cku-mes-{admin,bpm,lowcode}/src/fulgurjs`、根目录 `fulgurjs.config.ts`。

## 三应用当前联邦形态（4.1.0）

| 应用 | 容器名 | dev 端口 | base | 角色 | 接入方式 |
|---|---|---|---|---|---|
| cku-mes-admin | cku-mes-admin | 8773 | /main | 双角色 | `federation(federationOptionsForApp(await loadRepoConfig('../fulgurjs.config.ts')), 'cku-mes-admin')` |
| cku-mes-bpm | mes-bpm | 4529 | /flowable | 双角色（26 页 exposes + TaskCard + setup + 反向消费 admin） | 同上，app 名 'mes-bpm' |
| cku-mes-lowcode | mes-lowcode | 4669 | /lowcode | 纯远程（7 exposes + setup） | 同上 |

- 聚合配置 `testbed/mes-zc/fulgurjs.config.ts`：`defineRepoConfig({ root, apps[3] })`，admin 侧含 26 条页面表副本 + deriveSpec + remotePrefixes。
- devSharedSelf：admin 显式 true；bpm/lowcode 按 `hasExposes||setup` 推断为 true。
- shared：三应用均为 vue/vue-router/pinia singleton；**lowcode 额外 `element-plus: singleton ^2.10.2`**。
- 三应用 package.json 均装 `@fulgurjs/federation` 4.1.0。

## EP 版本与样式入口（BPM 标签栏问题背景）

| 应用 | element-plus 版本 | 乾坤入口样式 | 联邦入口（setup.ts→styles/index.scss） |
|---|---|---|---|
| admin（宿主） | 2.14.6（`registerThirdComp.ts` 全局引 `element-plus/dist/index.css`） | — | — |
| bpm | **2.9.1** | `plugins/elementPlus` 引 `theme-chalk/src/index.scss` 全量 | **仅 dark css-vars，无组件主题** |
| lowcode | 2.14.6 | 同上 | 同样仅 dark css-vars |

- EP 2.9.1 `el-tabs.css`：`.el-tabs--top{flex-direction:column-reverse}`（DOM=[content,header]，靠反转显示 header）。
- EP 2.14.6 `el-tabs.css`：`.el-tabs--top{flex-direction:column}` + `.el-tabs--top>.el-tabs__content+.el-tabs__header{order:-1}`（DOM=[header,content]）。
- 根因假设（待浏览器 computed style 定案）：8662 联邦详情页 DOM 为 BPM 自有 EP 2.9.1 产物，但生效 CSS 来自宿主 admin 的 2.14.6 全量主题（BPM 联邦入口缺自身主题 CSS）→ column-reverse 缺失 → header 按源顺序沉到内容后；其余组件因两版类名兼容而"看似正常"。

## 8662 站点

- nginx（homebrew）`30-fulgurjs-test-8662.conf`：listen 8662 → root `/opt/homebrew/var/www/fulgurjs-test`，location /main、/flowable、/lowcode（remoteEntry/manifest/index.html no-cache + CORS）。master PID 10824。服务运行中，**不停止**。

## 验证记录（本轮新增事实）

- （实施中逐步补记）
