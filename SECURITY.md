# 安全策略

## 支持的版本

仅最新主版本接收安全修复。

| 版本 | 状态 |
| --- | --- |
| 1.x | ✅ 接收安全修复 |
| < 1.0 | ❌ 已停止（升级到 1.x，跨应用传值改用 `@fulgurjs/federation/context`） |

## 上报漏洞

**请勿开公开 issue。** 用 GitHub 的私密漏洞上报通道：

👉 https://github.com/chenmingye/fulgurjs-federation/security/advisories/new

请附上：

- 受影响的版本（`@fulgurjs/federation` 版本 + 所在应用 dev 还是 prod）；
- 最小复现（配置片段 / 远程 entry / 触发步骤），以及是否可在真实部署形态复现；
- 影响判断：能读/写什么、是否需要受害者先执行某操作、是否需要攻击者控制某个远程地址。

## 在本项目语境下「安全问题」指什么

本包由两部分构成，攻击面也分两层：

- **浏览器运行时**（`dist/runtime.js`）：远程容器加载、shared 协商与版本裁决、`loadRemote` 的模块解析与错误处理。典型问题如远程 entry 的响应未经校验即被信任、版本裁决可被降级利用、错误信息泄漏敏感值。
- **构建期插件与 CLI**（`dist/index.js` / `dist/cli.js`，跑在开发者机器上）：源码转换、虚拟模块生成、配置解析、`doctor` 体检。典型问题如配置值注入、路径穿越写文件。

**明确的信任边界**：联邦是「同 realm 共存 + 依赖级隔离」架构，不做 JS 沙箱与 CSS 隔离——被加载的远程与宿主同权限执行，这是与 webpack Module Federation 一致的设计前提，**不是漏洞**（详见 [`docs/沙箱边界审计.md`](沙箱边界审计.md)）。因此「远程能拿到宿主的 window / 能读写宿主 DOM」属预期行为；而「远程本不该拿到的宿主凭据被拿到」「配置解析可被注入」等才在本项目范围内。

## 响应

个人维护项目，无 SLA；会尽快确认并给出修复或缓解建议，修复版本会记入 [CHANGELOG](CHANGELOG.md)。
